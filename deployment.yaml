apiVersion: apps/v1
kind: Deployment
metadata:
  name: herbayur-sms-email-service-deployment
spec:
  selector:
    matchLabels:
      app: herbayur-sms-email-service-app
  replicas: 1
  template:
    metadata:
      labels:
        app: herbayur-sms-email-service-app
    spec:
      containers:
      - name: herbayur-sms-email-service-server
        image: supunhansaka/herbayur-sms-email-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5007