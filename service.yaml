apiVersion: v1
kind: Service
metadata:
  name: herbayur-sms-email-service
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5007
      targetPort: 5007
  selector:
    app: herbayur-sms-email-service-app

# //service.yaml file
# -----------------------------
# services:

#   inventory:
#     git_url: https://github.com/HERBAYUR-Shopping-Platform-DS-Y3S2/herbayur-sms-email-service/tree/develop
#     git_branch: develop
#     command: npm start
#     build_root: .
#     ports:
#       - container: 5007
#         http: 50
#         https: 443
#     env_vars:
#       NODE_ENV: production

# databases:  
#   - mongodb
